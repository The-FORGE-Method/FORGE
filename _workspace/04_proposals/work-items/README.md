# Work Items

Active pipeline work-items are created here by `/forge-rd start`.

Each work-item follows the naming pattern: `YYYY-MM-DD-<feature-slug>/`

See `_workspace/README.md` for pipeline documentation.
